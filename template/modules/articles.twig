{% block articlesModule %}
    NewsID:{{ articles.newsID }}</br>
    Author:{{ articles.authorID }}</br>
    Article:{{ articles.article }}</br>
    Title:{{ articles.title }}</br>
    Category:{{ articles.category }}</br>
    Readed:{{ articles.readed }}</br>
    Uploaded:{{ articles.released }}</br></br>
{% endblock %}